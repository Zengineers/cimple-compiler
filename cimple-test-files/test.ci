program test
	# d e c l a r a t i o n s #
	declare x,p;
	declare i,fact;
	# m a i n #
	{	
		forcase
	         case (p<3) p:=p+5;
		     case (p<5) p:=p*2;
		     case (p>20) p:=p-3;
		     default p:=7*p;
	      ;
		switchcase
	         case (p<3) p:=p+5;
		     case (p<5) p:=p*2;
		     case (p>20) p:=p-3;
		     default p:=7*p;
	      ;
		
		while (i<=x)
		{
			fact := fact*i;
			i := i+1;
		};
		print(fact);
	}.